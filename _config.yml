theme: minima
title: Doitforonoreason's homepage
description: Where I occasionally post thoughts

plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Default layout for all pages
defaults:
  -
    scope:
      path: ""
    values:
      layout: default